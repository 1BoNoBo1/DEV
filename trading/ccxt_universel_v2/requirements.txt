# ---- Core ----
ccxt>=4.3.0
pandas>=2.2.0
PyYAML>=6.0.1

# ---- WebSockets (ccxt.pro inside ccxt) ----
aiohttp>=3.9.0
websockets>=12.0

# ---- Optional: columnar formats ----
pyarrow>=14.0.0        # Parquet/Feather (recommandé)
fastparquet>=2023.10.1 # Alternative Parquet (optionnel)
